<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivCheckmark"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_weight="0"
        android:layout_margin="@dimen/section_margin_normal"
        android:layout_gravity="center"
        app:tint="?attr/colorPrimaryMuted"
        app:srcCompat="@drawable/ic_check_circle_f_24px" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:layout_gravity="center_vertical"
        android:paddingVertical="@dimen/section_margin_normal"
        android:paddingEnd="@dimen/section_margin_normal">

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_title_xs"
            tools:text="Nadpis požadavku" />

        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/item_margin_small"
            tools:text="K čemu se to používá a proč je naprosto nezbytné, aby to bylo povolené." />
    </LinearLayout>
</LinearLayout>