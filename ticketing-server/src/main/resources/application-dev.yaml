server:
  ssl:
    key-store: "file:../pki/mos-ticketing-https-debug.pfx"
    key-store-password: "mosdevcert"
    trust-store: "file:../pki/ROPID_Root_CA_Certificate_[DEBUG].pfx"
    trust-store-password: "mosdevcert"

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mos_ticketing
    username: devel
    password: '-(-DONrS16RVCh-m'
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://iam.mos.ropid:8180/realms/mos

tickets:
  signing-key-path:
    pub: "../pki/Ticket_signing_key_pub.pem"
    priv: "../pki/Ticket_signing_key_priv.key"